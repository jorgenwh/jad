[project]
name = "jad-rl"
version = "0.1.0"
description = "Reinforcement learning agent for Jad prayer switching"
requires-python = ">=3.12"
dependencies = [
    "numpy>=2.4.0",
    "torch>=2.9.1",
]
